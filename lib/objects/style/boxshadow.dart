import 'package:flutter/material.dart';

const kBoxShadow = BoxShadow(
  color: Color.fromARGB(50, 0, 0, 0), // More transparent black
  spreadRadius: 0.5,
  blurRadius: 2,
  offset: Offset(0, 1), // Shadow direction: bottom
);
